# Deploy OnlyPorn 2.5.5

Deploy only after `npm run test:hotfix255` and `EXPECTED_VERSION=2.5.5 npm run validate:release` pass.

After Render becomes live, verify a non-empty SpankBang catalog and request a JAV HD Porn stream. The JAV response must contain an OnlyPorn `/media/` URL and the Render log must show a decoded reserve player followed by a registered HLS relay token.
