# Deploy OnlyPorn 2.5.4

Deploy only after the focused 2.5.4 test and the complete release validation
pass. After Render deploys, verify a JAVHDPorn stream request logs `jwSources`
greater than zero and returns at least one media relay URL.
